#include <stdio.h>
#include "jmp.h"

int main(){
	init_tab_jmp();
	add_from(31,JNE);
	add_from(51,JMP);
	print_tab();

	remplir(355);
	remplir(1651);
	print_tab();
	int type;
	printf("to : %d, type : %d\n",get_to(0,&type),type);

	

	return 0;
}
